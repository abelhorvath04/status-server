server:
  port: 8081

spring:
  application:
    name: status-service

eureka:
  client:
    instance:
      hostname: eureka-server
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
      register-with-eureka: true
      fetch-registry: true
# websocket:
#   endpoint: /ws
